# Versão 52

- Garante que os IDs `programacao`, `cancelamento` e `tef_elgin` sempre abram os formulários nativos.
- Corrige registros do Supabase gravados como `legacyType: custom` ou com tipo incorreto.
- Atualiza o cache do service worker para evitar a reutilização do JavaScript anterior.
- Inclui SQL de reparo dos três projetos padrão.
